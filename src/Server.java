import java.net.ServerSocket;
public class Server {
    private ServerSocket serverScocket;
    public Server(ServerSocket serverSocket){
        this.serverSocket = serverSocket;
    }
    public void startServer(){
        try{
            while(!serverScocket.isClosed()){
                
            }
        }
    }
}